# 2026-08-28 工作日志

## 重大结构变化：项目被重构为 pnpm monorepo（悬空半成品）

- **时间**：约 2026-08-28 19:41，非本 agent 所为（agent 上轮只改 web/ 4 文件未动结构）。
- **新结构（工作树，未提交）**：
  - `packages/core/`（`@survival/core`，仅 `dice.ts`/`rng.ts`/`resources.ts`/`types.ts` 共 372 行基础工具，空壳）
  - 根 `pnpm-workspace.yaml`（`packages/*` + `apps/*`）、`tsconfig.base.json`、改写 `package.json`（version 0.1.0，Vue3 + TS monorepo，`dev:web` 指向 `@survival/web`）
  - `apps/` 目录**未创建** → `pnpm dev:web` 当前跑不起来；迁移停在极早期。
- **旧结构全部 `D`（工作树删除，未暂存未提交）**：`web/`、`assets/`、`tools/`、`docs/` 及根 `tsconfig.json`、`DEV-NOTES.md`、`README.md`、`VERSION`、`启动游戏.bat` 等。
- **数据可恢复性**：
  - 旧 `04a5c60` 代码 = 100% 可恢复（`git checkout -- .` 从 04a5c60 重建，因删除未提交）。
  - 上轮（27日）未提交的 web UI 响应式重构（style.css/app.ts/main.ts/index.html）已随 `web/` 删除而丢失，改动逻辑记于 `2026-08-27.md` + `.workbuddy/artifacts/narrative-guide.md`，可重做。
- **未推送**：本地 `main` 有 3 提交未推远程（04a5c60/1915c79/44ea053），`origin/main` 指向更老提交。
- **风险**：确认意图前勿 `git commit` 删除 / `git reset --hard` / `git clean -fd`，否则旧 04a5c60 代码可能不可逆丢失。
- **待主人决策**：救回旧版（git checkout 恢复 04a5c60）/ 继续迁移（把逻辑·配置·UI 迁入 packages/core + apps/web）/ 保持现状先不动。

## 备注
- 本轮为「查看项目情况」请求，仅核查+汇报，未做任何改动（除本记忆文件）。
- 当前会话注入了「小台」专家，但本请求是项目状态查看，非工作台搭建，不套用其 workspace 流程；响应式双端适配规范已在上一轮 UI 重构中采纳。
