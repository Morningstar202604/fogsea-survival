# 2026-08-27 工作日志

## 版本重置 + 内容扩容（v0.1.x）
- 版本号重置为 v0.1.x 小版本体系：VERSION 0.5.0→0.1.0；README 状态段 v0.3.3→v0.1.0 并反映真实功能；DEV-NOTES 顶部加版本策略注记。历史 v0.2~v0.7.1 全部功能归并为 v0.1.0 起点（主人判定前期均为小版本）。
- v0.1.1 扩容（纯 JSON 配置，零代码改动、零假实现）：
  - 物品 +14（68 个）：肉干/水果罐头/热汤/薄荷茶/蜂蜜/镇痛贴/维生素片/兴奋剂/椰子水/老唱片(10 消耗品) + 兽骨/羽毛/玻璃片/野葱(4 材料)。全部使用合法 use 键，图标复用既有有效名避免破图。
  - 天赋 +10（T13–T22，22 个）：全部复用既有 12 种 hook 类型，自动进入抽卡池（draw3 用 cfg.talents 全量），单天赋模型下真实生效。
  - 配方 +8（33 个）：风干肉干/蜜渍果/煮热汤/薄荷茶/镇痛贴/维生素片/兴奋剂/布绳。产物均为既有或本批新增物品，前置设施仅 campfire。
- 三真源同步：assets/resources/configs → web/dist/configs + web/android/app/src/main/assets/public/configs（消除 web/dist 缺失 craft_crystal_lamp 的历史快照差异）。
- 关键事实核查：TalentRuntime 是 per-talent 实例（单天赋模型）、天赋池=全量 talents.json、ConfigSchema 不校验 hook 类型枚举（仅校验引用完整性）。新增内容 ConfigSchema 三处 0 issue。
- 回归：tools vitest 55/55（修复 tests/configs.test.ts 过时底线 12→22）、web tsc 0 错误 + vitest 3/3、三真源 ConfigSchema 0 issue。
- Cocos tsc 未跑：本机缺 TS CLI（npx 误装 tsc@2.0.3 假包）+ Cocos 端冻结在 v0.3.x + 本次仅改纯 JSON（不进类型检查），属环境项非回归。

## 待办 / 提醒
## v0.1.2 剧本线（救援线 + 结晶真相线）
- 场景剧本引擎新增两条 8 幕角色剧本，纯 JSON 零代码改动、零假实现：救援线 rescue_s1_wreck~rescue_s8_leave（8 幕，rescue NPC 好感 + 赠 key_radio_parts 助建 radio → 推进原生 rescueProgress → 衔接 E13 守望者的日记）、结晶真相线 crystal_s1_vein~crystal_s8_truth（8 幕，laok/doc 揭示真相 + 赠 key_mist_crystal 衔接 E05 迷雾之眼，maxStack=3 契合三块收集）。
- 真实场景 id（与摘要近似写法不同，已用 node 核对）：rescue = wreck/awake/radio/coord/split/tide/eve/leave；crystal = vein/laok/resonate/doc/second/third/crack/truth。场景总数 28→44，id 重复 0。
- 三真源同步已用脚本核验：assets/resources/configs/scenes.json = web/public/configs/scenes.json = web/android/.../public/configs/scenes.json，三处均 44 场、44 唯一 id、16 新 id 全在（ALL_SOURCES_OK）。注：web/android 被 gitignore（Capacitor 壳），同步仅用于本地 APK 出包，不进 git。
- 零假实现核查：rescue NPC 已在 RelationshipSystem 初始化好感(rescue:0)；key_mist_crystal maxStack=3 与 E05 触发事件(fog_story.json)一致；craft_radio 产出 radio 设施。
- 回归（前序轮已跑）：tools vitest 58/58（新增 verify_scenes.test 全量 ConfigSchema 0 issue）· web tsc 0 · web vitest 3/3 · 模拟器 56.1%/43.9% 双达标。

- tools/audit_paths.mjs 仍未被 git 跟踪（历史遗留），本轮仍排除在提交外，建议确认后入仓或删除。
- v0.1.0 + v0.1.1 + v0.1.2 已于 04a5c60 本地提交（35 文件，+11266/-618）；未 push 远程，待主人确认后推送（GitCode/GitHub/Gitee）。

## Web HUD 重构（暗色迷雾风，回应"丑/空/功能不足"）
- 证据化诊断：原 HUD 用 ASCII 方块 `█░` 画属性条、日志 `logLines` 上限仅 4 行、状态只显示原始 `[id]`、世界状态「道德/雾压」(`ctx.run.counters.morality/fogPressure`) 完全没渲染。
- 改动（`web/src/style.css` + `web/src/app.ts`，纯前端无数据/逻辑变更）：① 顶部状态条(天数/天气/庇护所/背包/技能)；② 四维属性改 CSS 渐变进度条(按阈值变红/金/绿)；③ 新增世界状态条(🪞道德 0-100 / 🌫雾压 0-30)；④ 状态改中文药丸(`cfg.statuses` 映射，buff绿/debuff红)；⑤ 叙事日志改可滚动 DOM 行(上限 60，自动滚底)；⑥ 行动区分组标题(外出探索/日常经营)；⑦ 暗色渐变氛围 + 按钮 hover。
- 验证：本地 `tsc --noEmit` 0 错误；`cfg.statuses` 加 `?.` 防御；vitest 初跑 2 failed(因天数格式 `第1天`→`第 1 天` 致断言过期)，已统一回「第N天」约定(撤销 topbar 与晨间日志多余空格)，重跑门禁后台进行(EwA7nP)。
- 注：本机无 Playwright 浏览器(装它会吃 C 盘)，改用 jsdom 冒烟测试(boot/night-flow)作质量门禁；dev server 仍在后台 ClMuUU 运行，预览可看实时效果。

## Web 界面响应式全量重构（本会话完成，回应"全部重构/电脑手机都能用"）
- **根因**：原 `#phone{max-width:480px}` 假手机壳 + 全站单列纵向 flex 锁死成 PC 中央 480px 窄带——与主人"界面完全错误/只限手机端"判断吻合。
- **改动**：`web/index.html` 根 `id="phone"`→`id="app"` 并移除 `user-scalable=no`；`web/src/main.ts:19` 挂载点同步改 `'app'`(不改会白屏)；`web/src/style.css` 全量重写(暗色雾霭风 + 三断点 Grid：≥1100 三栏/760–1099 两栏/<760 单列/<480 行动单列)；`web/src/app.ts` `buildGameScreen` 三栏 + `HudRefs.relBox` + `renderRelations`(5 NPC 好感条 + 随从卡 + 天赋卡，全读真实字段零假数据；RunState 无 relationships 字段，关系从 `counters['rel_'+npc]` 读)。
- **验证**：`web` 包 `tsc --noEmit` exit 0；前轮 web vitest 3/3 绿；dev server 5173 返回 200 且含 `id="app"`(无白屏)。任务 13–18 均 completed。4 文件(index.html/app.ts/main.ts/style.css)为未提交工作树改动。

## 叙事写法调研 + 界面布局二次优化（本回合）
- 诉求：① 说明剧情"怎么写"并概括整款游戏剧情(基于真实配置非臆造)；② 优化界面布局。
- 证据化调研：剧情=两层——`scenes.json`(44 场手写多幕剧本, ink weave JSON化: node.text+choices+goto/gather 汇流) + `events/fog_*.json`(事件池分支叙事, type+weight 抽取)。schema 来自 `SceneDefs.ts`/`EventDefs`；效果落地经 `effects`(setFlags/relNpc+relDelta/morality/sanity/fogPressure/gainItems/loseItems/endingId)。关系 5 NPC 落 `counters['rel_'+npc]`(RunState 无 relationships 字段)。结局 14 个(good4/hidden4/death6)。
- 产出 `.workbuddy/artifacts/narrative-guide.md`：写法手册(两层结构/SceneDef+EventDef 字段+真实样本/世界状态维度/6线44场+14结局剧情总览/批量扩写清单)，供主人后续照抄产内容。
- 布局二次优化(仅 CSS，零逻辑变更、零回归)：① **修手机端裁切 bug**——`<760px` 主屏改 `height:100%+overflow-y:auto`(原 `height:auto` 被 `#app{overflow:hidden}` 裁切、行动按钮够不到)，并加 `.col-actions` sticky 吸底常驻；② 中栏 `.col-story` 限宽 860px 居中(超宽屏不再拉 1100px+ 长行)；③ `.logpanel` 行距 1.7→1.82、字号 15→15.5、加 `text-wrap:pretty`。验证：tsc exit 0；CSS 括号 112/112 平衡；dev server style.css 200。
- style.css 改动叠加在上一轮 UI 未提交改动之上；git 提交/push 仍待主人确认。
